//TODO DB cleanup function, runs periodically
// remove images no longer referenced by any article
// inactive (for 1 year) users and all their data
// textbooks/courses that have no owner (owner user deleted)
// old session tokens
// old logs (more than 3 months)
