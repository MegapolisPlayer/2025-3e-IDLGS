//TODO bookmarks and highlights

import type { BookmarkType, UserType, HighlightType } from "$lib/types";

export const loadBookmarks = async (user: UserType): Promise<BookmarkType[]> => {
	return [];
};

export const loadHighlights = async (user: UserType): Promise<HighlightType[]> => {
	return [];
};
